syntax = "proto3";

package foxy_grpc.protocol;

message StringRequest {
    uint32 count = 1;
}

message StringResponse {
    string content = 1;
}

service StringService {
    rpc GetStrings(StringRequest) returns (stream StringResponse) {}
}
